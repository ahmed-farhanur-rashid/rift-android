<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <include domain="file" path="." />
        <!-- Uncomment both lines below to back up all databases except rift.db -->
        <!-- <include domain="database" path="." /> -->
        <!-- <exclude domain="database" path="rift.db" /> -->
    </cloud-backup>
    <device-transfer>
        <include domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
