*.iml
/local.properties
/.idea/
.DS_Store
/captures
.externalNativeBuild
.cxx
local.properties

# Release keystore — NEVER commit this
release.keystore
*.jks
*.keystore

# Build outputs
/build
/app/build/

# Virtual environment & IDE-specific files
venv/
.venv/
.vscode/
.kotlin/
.gradle/

# Java heap dumps — NEVER commit these
*.hprof

# AWID3 raw dataset (academic use only — do not commit)
/ml/dataset/awid3/

# ML intermediate training artifacts (NOT the final models in assets/)
/ml/models/*.pkl
/ml/models/*.onnx
/ml/models/*.onnx.data
/ml/models/*.pt
/ml/training/*.pkl
/ml/training/*.onnx
/ml/training/*.pt
__pycache__/

# Signing info
release.txt
release_clean.txt