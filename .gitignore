*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/captures
.externalNativeBuild
.cxx
local.properties

# Release keystore — NEVER commit this
release.keystore
*.jks
*.keystore

# Build outputs
/build
/app/build/

# Virtual environment
venv/
.venv/

# AWID3 raw dataset (academic use only — do not commit)
/ml/dataset/awid3/

# ML intermediate training artifacts (NOT the final models in assets/)
/ml/training/awid3_*.csv
/ml/training/*.pkl
/ml/training/*.onnx
/ml/training/*.onnx.data
/ml/training/*.pt
__pycache__/
